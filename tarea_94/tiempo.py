def tierra(t):
    z = (1/2) * 9.8 * (int(t) * 2)
    return f"{z:.2f}"

def marte(t):
    z = (1 / 2) * 3.7 * (int(t) * 2)
    return f"{z:.2f}"

def jupiter(t):
    z = (1 / 2) * 24.8 * (int(t) * 2)
    return f"{z:.2f}"
